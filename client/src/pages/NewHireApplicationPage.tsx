import { Navigate, useParams } from 'react-router-dom'
import ApplicationForm from '../components/ApplicationForm'
import PageHero from '../components/PageHero'
import type { Language } from '../context/language'

export default function NewHireApplicationPage(){const{language}=useParams();if(language!=='en'&&language!=='es')return <Navigate to="/forms" replace/>;const es=language==='es';return <><PageHero eyebrow={es?'Incorporación':'Onboarding'} title={es?'Solicitud de Nuevo Empleado':'New Hire Application'} text={es?'Complete este formulario solo después de recibir instrucciones de BZ Resources.':'Complete this form only after BZ Resources has instructed you to do so.'} meta={es?'Formulario seguro':'Secure form'}/><section className="section form-page-section"><div className="container-form"><div className="privacy-callout">{es?'No envíe SSN, documentos de identidad, datos médicos, bancarios o fiscales en este formulario. Use el proceso seguro indicado por BZ Resources.':'Do not submit SSNs, identity documents, medical, banking or tax information in this form. Use the secure process provided by BZ Resources.'}</div><ApplicationForm kind="new-hire-application" language={language as Language}/></div></section></>}
